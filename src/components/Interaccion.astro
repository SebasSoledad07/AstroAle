---

---
<section class="text-center my-10">
    
    <div id="message-container">
      <button id="reveal-btn" class="bg-pink-500 hover:bg-pink-600 text-white px-6 py-3 rounded-xl">
        Revelar Mensaje Especial
      </button>
    </div>
    
    <div id="special-message" class="hidden text-purple-800 text-xl mt-6 flex flex-col items-center justify-center">
      <p class="mb-4">
        ✨ Siempre estaré contigo, mi amor. Gracias por estos momentos tan hermosos. 💖
      </p>
    </div>
  </section>

  <section class="text-center my-10">
  <a href="https://www.youtube.com/watch?v=2h9pIhTocOk" target="_blank" rel="noopener noreferrer">
  <button class="bg-purple-600 hover:bg-purple-700 text-white px-6 py-3 rounded-xl">
    Escuchala mi amor 🍺
  </button>
</a>
  </section>
  <style>
    @keyframes fadeIn {
  from {
    opacity: 0;
    transform: scale(0.95);
  }
  to {
    opacity: 1;
    transform: scale(1);
  }
}

.animate-fadeIn {
  animation: fadeIn 0.6s ease-out forwards;
}
  </style>

<script is:inline>
    window.addEventListener('DOMContentLoaded', () => {
      const revealBtn = document.getElementById('reveal-btn');
      const messageContainer = document.getElementById('message-container');
      const specialMessage = document.getElementById('special-message');
  
      if (revealBtn && messageContainer && specialMessage) {
        revealBtn.addEventListener('click', () => {
          messageContainer.classList.add('hidden');
          specialMessage.classList.remove('hidden');
          specialMessage.classList.add('animate-fadeIn');
        });
      }
    });
    const musicBtn = document.getElementById('music-btn');
  const player = document.getElementById('youtube-player');

  musicBtn.addEventListener('click', () => {
    player.src = 'https://www.youtube.com/watch?v=iRi4_Yq--Ok';
    player.classList.remove('hidden');
  });
  </script>